import re

def extract_during_numbers(s):
    pattern = r'during:\s*\[(\d+)\]'  # 匹配 "during: [数字]"
    return re.findall(pattern, s)  # 返回所有匹配的数字

# 测试
s = "开始一些文本 during: [60] 结束一些文本 during: [120] 和更多文本"
numbers = extract_during_numbers(s)
print(numbers)  # 输出: ['60', '120']
